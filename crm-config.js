/* Public CRM gateway for live claim lookup.
   Must point at the PC proxy (serve-all /claim) via a public tunnel while VPN is on.
   Static GitHub Pages cannot reach Methaq CRM directly. */
window.METHAQ_CRM_GATEWAY = window.METHAQ_CRM_GATEWAY || "";
